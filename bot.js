// bot qui permet d'acheter automatiquement des personnages
/*document.addEventListener("DOMContentLoaded",() => {
const bot = ()=> {

    let monOr = document.getElementById('affGold').innerHTML;
    console.log(monOr)

       if (monOr >= paysanSerpette.prix && paysanSerpette.nombre !== 20) {
        acheterPaysan();
       }
       if (monOr >= soldatEpee.prix && soldatEpee.nombre !== 15) {
        acheterSoldatEpee();
       }
       if (monOr >= archer.prix && archer.nombre !== 22) {
        acheterArcher();
       }
       if (monOr >= lanceur.prix && lanceur.nombre !== 15) {
        acheterLanceur();
       }
    };
setInterval(bot,100);
});*/
